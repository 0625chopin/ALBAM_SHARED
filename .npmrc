# @0625chopin 스코프는 GitHub Packages 레지스트리 사용
# 토큰은 커밋 금지 — 환경변수 ${NPM_TOKEN}로 치환됨(발행 시 write:packages, 설치 시 read:packages)
@0625chopin:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
