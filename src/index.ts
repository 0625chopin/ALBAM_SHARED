// @0625chopin/shared 진입 placeholder
// 실제 공유 소스(types / database.types / supabase/* / format / utils / constants /
// queries/map / ui/* / common/* / styles.css)는 TS06(공유 소스 이관)에서 채워진다.
// 소비 앱은 배럴(root)이 아니라 subpath export로 가져온다: 예) @0625chopin/shared/types
export const SHARED_PACKAGE = "@0625chopin/shared" as const;
